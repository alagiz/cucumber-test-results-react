language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - cd komkommer-react
  - npm install
  - npm test -- --coverage
  - npm run build
after_script:
  - COVERALLS_REPO_TOKEN=$coveralls_repo_token npm run coveralls